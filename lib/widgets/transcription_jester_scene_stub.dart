import 'package:flutter/material.dart';

class TranscriptionJesterScene extends StatelessWidget {
  const TranscriptionJesterScene({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.expand();
}
