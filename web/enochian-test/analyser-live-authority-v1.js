(()=>{
'use strict';
const VERSION='20260830-input-main-fx-v5';
const cloneBytes=arr=>arr instanceof Uint8Array?new Uint8Array(arr):Uint8Array.from(arr||[]),clampByte=v=>Math.max(0,Math.min(255,Math.round(v||0)));
function inner(host){try{const live=host?.contentDocument,deck=live?.getElementById('deck');return{d:deck?.contentDocument,w:deck?.contentWindow}}catch(_){return{}}}
function ensureBusHooks(d,w){const bus=w.__enochAnalyserBus;if(!bus)return false;let repaired=false;const currentSignal=w.signal;if(typeof currentSignal==='function'&&!currentSignal.__enochBusWrapped&&!currentSignal.__enochLiveAuthorityWrapped){const original=currentSignal,wrapped=function(frameData){if(frameData&&frameData.length){bus.frequency=cloneBytes(frameData);bus.frequencyFrame=(bus.frequencyFrame||0)+1;bus.emit?.('frequency',bus.frequency)}const vals=original.apply(this,arguments);if(Array.isArray(vals)&&vals.length>=4){bus.rawSignal=vals.slice(0,4).map(clampByte);bus.signalFrame=(bus.signalFrame||0)+1;bus.emit?.('signal',bus.rawSignal)}return vals};wrapped.__enochLiveAuthorityWrapped=true;wrapped.__original=original.__original||original;w.signal=wrapped;repaired=true}const currentDraw=w.draw;if(typeof currentDraw==='function'&&!currentDraw.__enochBusWrapped&&!currentDraw.__enochLiveAuthorityWrapped){const original=currentDraw,wrapped=function(canvas,arr){if(arr&&arr.length){bus.waveform=cloneBytes(arr);bus.waveFrame=(bus.waveFrame||0)+1;bus.emit?.('waveform',bus.waveform)}return original.apply(this,arguments)};wrapped.__enochLiveAuthorityWrapped=true;wrapped.__original=original.__original||original;w.draw=wrapped;repaired=true}d.documentElement.dataset.analyserBusHooks=(typeof w.signal==='function'&&(w.signal.__enochBusWrapped||w.signal.__enochLiveAuthorityWrapped))?'live':'pending';return repaired||d.documentElement.dataset.analyserBusHooks==='live'}
function loadSculpt(host){if(window.installEnochianMultipointSculptV1){window.installEnochianMultipointSculptV1(host);return}if(document.getElementById('enoch-multipoint-sculpt-loader'))return;const s=document.createElement('script');s.id='enoch-multipoint-sculpt-loader';s.src='/enochian-test/analyser-multipoint-sculpt-v1.js?v=20260827-v2';s.onload=()=>window.installEnochianMultipointSculptV1?.(host);document.head.appendChild(s)}
function installInputLoop(d,w){
 if(w.__enochLiveInputLoop?.version===VERSION)return true;
 if(!w.navigator?.mediaDevices?.getUserMedia)return false;
 const audio=d.getElementById('audio');if(!audio)return false;
 let bar=d.getElementById('enoch-live-input-bar');
 if(!bar){bar=d.createElement('section');bar.id='enoch-live-input-bar';bar.className='enoch-live-input-bar';bar.setAttribute('aria-label','Live audio input and loop recorder');bar.innerHTML='<span class="eli-title">INPUT</span><select data-eli-device aria-label="Audio input"><option value="">DEFAULT INPUT</option></select><button type="button" data-eli-start>START INPUT</button><button type="button" data-eli-rec disabled>REC LOOP</button><button type="button" data-eli-play disabled>PLAY LOOP</button><button type="button" data-eli-clear disabled>CLEAR</button><span data-eli-status>INPUT OFF</span>';const anchor=d.querySelector('.wave')||d.querySelector('.app')||d.body;anchor.parentNode?.insertBefore(bar,anchor)}
 let style=d.getElementById('enoch-live-input-loop-style');if(!style){style=d.createElement('style');style.id='enoch-live-input-loop-style';style.textContent='#enoch-live-input-bar{display:flex;align-items:center;gap:6px;min-height:34px;padding:4px 8px;margin:0 0 6px;border:1px solid rgba(57,200,245,.38);background:linear-gradient(180deg,rgba(4,18,27,.96),rgba(0,4,8,.96));font:700 10px/1.1 ui-monospace,SFMono-Regular,Consolas,monospace;letter-spacing:.08em;color:#8fe8ff;box-sizing:border-box;overflow:hidden}#enoch-live-input-bar .eli-title{color:#ff9f2f}#enoch-live-input-bar select,#enoch-live-input-bar button{height:24px;border:1px solid rgba(57,200,245,.35);background:#061018;color:#b9f1ff;font:700 9px ui-monospace,SFMono-Regular,Consolas,monospace;padding:0 7px}#enoch-live-input-bar select{min-width:150px;max-width:260px;flex:1}#enoch-live-input-bar button{cursor:pointer;white-space:nowrap}#enoch-live-input-bar button:disabled{opacity:.35;cursor:default}#enoch-live-input-bar button[data-state="on"],#enoch-live-input-bar button[data-state="rec"]{border-color:#ff9f2f;color:#ffc16e;box-shadow:0 0 9px rgba(255,159,47,.22)}#enoch-live-input-bar [data-eli-status]{min-width:110px;text-align:right;color:#79d9ef;white-space:nowrap}@media(max-width:760px){#enoch-live-input-bar{gap:3px;padding:3px 4px;flex-wrap:wrap}#enoch-live-input-bar select{min-width:120px;max-width:190px}#enoch-live-input-bar button{padding:0 5px;font-size:8px}#enoch-live-input-bar [data-eli-status]{min-width:auto;flex:1;text-align:right}}';d.head.appendChild(style)}
 const q=s=>bar.querySelector(s),device=q('[data-eli-device]'),start=q('[data-eli-start]'),rec=q('[data-eli-rec]'),play=q('[data-eli-play]'),clear=q('[data-eli-clear]'),status=q('[data-eli-status]');
 let stream=null,recorder=null,chunks=[],loopBlob=null,loopUrl='',inputActive=false,recording=false,loopPlaying=false;
 let saved={src:audio.currentSrc||audio.src||'',time:Number(audio.currentTime)||0,loop:!!audio.loop};
 const setStatus=t=>{status.textContent=t;d.documentElement.dataset.liveInputStatus=t.toLowerCase().replace(/\s+/g,'-')};
 const ensureMain=async()=>{if(typeof w.ensureAudio==='function')await w.ensureAudio();return true};
 const rememberDeck=()=>{if(!inputActive&&!loopPlaying)saved={src:audio.currentSrc||audio.src||'',time:Number(audio.currentTime)||0,loop:!!audio.loop}};
 const restoreDeck=async()=>{try{audio.pause();audio.srcObject=null;audio.loop=saved.loop;if(saved.src){audio.src=saved.src;audio.load();if(saved.time>0){const seek=()=>{try{audio.currentTime=Math.min(saved.time,Number.isFinite(audio.duration)?audio.duration:saved.time)}catch(_){};audio.removeEventListener('loadedmetadata',seek)};audio.addEventListener('loadedmetadata',seek)}}}catch(_){}d.documentElement.dataset.analyserLiveSource='idle'};
 const refreshDevices=async()=>{try{const all=await w.navigator.mediaDevices.enumerateDevices(),inputs=all.filter(x=>x.kind==='audioinput'),keep=device.value;device.innerHTML='<option value="">DEFAULT INPUT</option>';inputs.forEach((x,i)=>{const o=d.createElement('option');o.value=x.deviceId;o.textContent=x.label||`AUDIO INPUT ${i+1}`;device.appendChild(o)});if([...device.options].some(o=>o.value===keep))device.value=keep}catch(_){setStatus('DEVICE ERROR')}};
 const stopStream=async(restore=true)=>{stream?.getTracks?.().forEach(t=>t.stop());stream=null;inputActive=false;start.textContent='START INPUT';start.dataset.state='';rec.disabled=true;d.documentElement.dataset.liveInput='off';if(restore&&!loopPlaying)await restoreDeck()};
 const startInput=async()=>{try{rememberDeck();await stopStream(false);await ensureMain();const id=device.value;stream=await w.navigator.mediaDevices.getUserMedia({audio:{deviceId:id?{exact:id}:undefined,echoCancellation:false,noiseSuppression:false,autoGainControl:false},video:false});audio.pause();audio.removeAttribute('src');audio.srcObject=stream;audio.loop=false;await audio.play();inputActive=true;loopPlaying=false;start.textContent='STOP INPUT';start.dataset.state='on';rec.disabled=false;d.documentElement.dataset.liveInput='on';d.documentElement.dataset.analyserLiveSource='live-input-main-fx';d.documentElement.dataset.analyserLiveHealth='live';setStatus('INPUT → EQ/FX');await refreshDevices()}catch(e){await stopStream(true);setStatus(e?.name==='NotAllowedError'?'PERMISSION NEEDED':'INPUT ERROR')}};
 const stopRecording=()=>{if(recorder&&recording&&recorder.state!=='inactive')recorder.stop()};
 const beginRecording=()=>{if(!stream||!inputActive)return;try{chunks=[];const type=['audio/webm;codecs=opus','audio/webm','audio/mp4'].find(x=>w.MediaRecorder?.isTypeSupported?.(x))||'';recorder=new w.MediaRecorder(stream,type?{mimeType:type}:undefined);recorder.ondataavailable=e=>{if(e.data?.size)chunks.push(e.data)};recorder.onstop=()=>{recording=false;rec.textContent='REC LOOP';rec.dataset.state='';if(chunks.length){loopBlob=new Blob(chunks,{type:recorder.mimeType||'audio/webm'});if(loopUrl)URL.revokeObjectURL(loopUrl);loopUrl=URL.createObjectURL(loopBlob);play.disabled=false;clear.disabled=false;setStatus('LOOP READY')}else setStatus('NO LOOP DATA')};recorder.start(100);recording=true;rec.textContent='STOP REC';rec.dataset.state='rec';setStatus('RECORDING INPUT')}catch(_){setStatus('REC ERROR')}};
 const stopLoop=async()=>{if(!loopPlaying)return;audio.pause();loopPlaying=false;play.textContent='PLAY LOOP';play.dataset.state='';if(inputActive){audio.removeAttribute('src');audio.srcObject=stream;audio.loop=false;try{await audio.play()}catch(_){}d.documentElement.dataset.analyserLiveSource='live-input-main-fx';setStatus('INPUT → EQ/FX')}else{await restoreDeck();setStatus('LOOP READY')}};
 const playLoop=async()=>{if(!loopUrl)return;try{if(loopPlaying){await stopLoop();return}await ensureMain();audio.pause();audio.srcObject=null;audio.src=loopUrl;audio.loop=true;await audio.play();loopPlaying=true;play.textContent='STOP LOOP';play.dataset.state='on';d.documentElement.dataset.analyserLiveSource='input-loop-main-fx';d.documentElement.dataset.analyserLiveHealth='live';setStatus('LOOP → EQ/FX')}catch(_){setStatus('LOOP ERROR')}};
 const clearLoop=async()=>{await stopLoop();if(loopUrl)URL.revokeObjectURL(loopUrl);loopUrl='';loopBlob=null;chunks=[];play.disabled=true;clear.disabled=true;setStatus(inputActive?'INPUT → EQ/FX':'INPUT OFF')};
 start.addEventListener('click',async()=>{if(inputActive){await stopStream(true);setStatus(loopBlob?'LOOP READY':'INPUT OFF')}else await startInput()});
 device.addEventListener('change',()=>{if(inputActive)startInput()});rec.addEventListener('click',()=>recording?stopRecording():beginRecording());play.addEventListener('click',playLoop);clear.addEventListener('click',clearLoop);w.navigator.mediaDevices.addEventListener?.('devicechange',refreshDevices);refreshDevices();
 w.__enochLiveInputLoop={version:VERSION,start:startInput,stop:stopStream,record:beginRecording,stopRecord:stopRecording,playLoop,stopLoop,clear:clearLoop,state:()=>({inputActive,recording,hasLoop:!!loopBlob,loopPlaying,deviceId:device.value,route:(inputActive||loopPlaying)?'main-eq-fx':'deck'})};
 w.addEventListener('pagehide',()=>{try{stopRecording()}catch(_){}stream?.getTracks?.().forEach(t=>t.stop());if(loopUrl)URL.revokeObjectURL(loopUrl)},{once:true});return true;
}
function install(host){const{d,w}=inner(host);if(!d||!w||!d.body||!w.__enochAnalyserBus||!w.__enochAnalyser3D)return false;const wave=d.querySelector('.wave'),real=wave?.querySelector('.analyser-3d');if(!wave||!real)return false;let style=d.getElementById('analyser-live-authority-v1-style');if(!style){style=d.createElement('style');style.id='analyser-live-authority-v1-style';style.textContent='.wave .analyser-3d{display:block!important;opacity:.96!important;filter:saturate(.98) drop-shadow(0 0 3px rgba(57,200,245,.18))!important;z-index:4!important;pointer-events:none!important}.wave .enoch-reference-mesh,.wave .enoch-reference-rim{display:none!important;opacity:0!important;pointer-events:none!important}.wave{touch-action:none!important}html[data-analyser-live-health="stale"] .wave .analyser-3d{opacity:.18!important}';d.head.appendChild(style)}ensureBusHooks(d,w);loadSculpt(host);const bus=w.__enochAnalyserBus;installInputLoop(d,w);if(d.documentElement.dataset.analyserLiveAuthority===VERSION)return true;const audio=d.getElementById('audio');let lastFrame=Number(bus.frequencyFrame)||0,lastAdvance=performance.now(),timer=0;const audit=()=>{ensureBusHooks(d,w);loadSculpt(host);installInputLoop(d,w);const frame=Number(bus.frequencyFrame)||0,now=performance.now(),playing=!!audio&&!audio.paused&&!audio.ended,input=w.__enochLiveInputLoop?.state?.()||{};if(frame!==lastFrame){lastFrame=frame;lastAdvance=now}const sourceActive=input.inputActive||input.loopPlaying,healthy=sourceActive||!playing||now-lastAdvance<900;if(sourceActive){d.documentElement.dataset.analyserLiveHealth='live'}else{d.documentElement.dataset.analyserLiveHealth=healthy?'live':'stale';d.documentElement.dataset.analyserLiveSource=playing?(healthy?'master-fft':'awaiting-fft'):'idle'}if(!healthy)ensureBusHooks(d,w);timer=w.setTimeout(audit,250)};d.documentElement.dataset.analyserLiveAuthority=VERSION;w.__enochAnalyserLiveAuthority={version:VERSION,repair:()=>ensureBusHooks(d,w),health:()=>({status:d.documentElement.dataset.analyserLiveHealth,source:d.documentElement.dataset.analyserLiveSource,frequencyFrame:Number(bus.frequencyFrame)||0,input:w.__enochLiveInputLoop?.state?.()})};audit();w.addEventListener('pagehide',()=>{if(timer)w.clearTimeout(timer)},{once:true});return true}
function boot(){const host=document.getElementById('terminalLive');if(!host)return;let tries=0,t=setInterval(()=>{if(install(host)||++tries>320)clearInterval(t)},50);host.addEventListener('load',()=>{tries=0;t=setInterval(()=>{if(install(host)||++tries>320)clearInterval(t)},50)},{once:true})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();window.installEnochianAnalyserLiveAuthorityV1=install;
})();