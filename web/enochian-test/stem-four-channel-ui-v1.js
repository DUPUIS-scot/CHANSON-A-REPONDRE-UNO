(()=>{
  'use strict';
  function install(frame){
    try{
      const live=frame?.contentDocument,deck=live?.getElementById('deck'),d=deck?.contentDocument,w=d?.defaultView;
      const iso=d?.querySelector('.stem-isolator'),engine=w?.__enochNativeStemEngine;if(!d||!w||!iso||!engine)return false;
      if(d.documentElement.dataset.stemFourChannel==='v4')return true;
      const keys=['vocals','drums','bass','other'],clamp=v=>Math.max(0,Math.min(1,Number(v)||0));
      const instBtn=iso.querySelector('[data-stem-toggle="instruments"]'),instRange=iso.querySelector('[data-stem-range="instruments"]'),instRow=instBtn?.closest('.stem-row')||instRange?.closest('.stem-row');
      const template=iso.querySelector('[data-stem-toggle="vocals"]')?.closest('.stem-row')||iso.querySelector('[data-stem-toggle="drums"]')?.closest('.stem-row')||instRow;if(!template)return false;
      const makeRow=(key,label)=>{const row=template.cloneNode(true),btn=row.querySelector('.stem-toggle'),range=row.querySelector('.stem-range'),value=row.querySelector('.stem-value');if(!btn||!range)return null;btn.dataset.stemToggle=key;btn.textContent=label;btn.className='btn stem-toggle';btn.setAttribute('aria-label',label+' stem on or off');range.dataset.stemRange=key;range.className='stem-range';range.setAttribute('aria-label',label+' stem level');if(value){value.dataset.stemValue=key;value.className='stem-value'}return row};
      if(instRow){const bass=makeRow('bass','BASS'),other=makeRow('other','OTHER');if(bass&&other)instRow.replaceWith(bass,other)}
      iso.querySelectorAll('[data-stem-jecker-split],.stem-jecker-split').forEach(n=>n.remove());[...iso.querySelectorAll('.stem-row')].forEach(row=>{const key=row.querySelector('[data-stem-toggle]')?.dataset.stemToggle;if(key&&!keys.includes(key))row.remove()});
      const note=iso.querySelector('.stem-note');if(note)note.textContent='VOCALS · DRUMS · BASS · OTHER · LOCKED TO MASTER TRANSPORT';
      let master=d.getElementById('stemMasterToggle');
      if(master){let header=iso.querySelector('.stem-primary-header');if(!header){header=d.createElement('div');header.className='stem-primary-header';const title=iso.querySelector('.stem-title');if(title)title.replaceWith(header);else iso.prepend(header)}let label=header.querySelector('.stem-primary-title');if(!label){label=d.createElement('span');label.className='stem-primary-title';label.textContent='STEMS MIX';header.appendChild(label)}master.classList.add('stem-master-primary');master.textContent='STEMS';master.title='STEMS MASTER · activate or mute all stems';master.setAttribute('aria-label','STEMS MASTER activate or mute all stems');if(master.parentElement!==header)header.appendChild(master)}
      const oldActions=d.querySelector('.terminal-actions');if(oldActions){[...oldActions.querySelectorAll('button,[role=button]')].forEach(btn=>{if(btn===master)return;const text=(btn.textContent||'').trim().toUpperCase();if(text==='FULL SCREEN'||btn.matches('[data-terminal-fullscreen]'))btn.remove()})}
      let style=d.getElementById('stem-four-channel-style');if(style)style.remove();style=d.createElement('style');style.id='stem-four-channel-style';style.textContent='.stem-isolator{position:relative!important}.stem-primary-header{display:grid!important;grid-template-columns:minmax(0,1fr) auto!important;align-items:center!important;gap:8px!important;min-height:30px!important;padding:0 0 5px!important;border-bottom:1px solid #17332e!important}.stem-primary-title{color:#f1a72d!important;font:800 9px/1 ui-monospace,SFMono-Regular,Menlo,monospace!important;letter-spacing:.16em!important}.stem-master-primary{position:relative!important;inset:auto!important;transform:none!important;z-index:30!important;display:inline-flex!important;visibility:visible!important;opacity:1!important;pointer-events:auto!important;min-width:72px!important;min-height:27px!important;padding:5px 9px!important;margin:0!important;white-space:nowrap!important}.stem-isolator .stem-row{display:grid!important;grid-template-columns:88px minmax(0,1fr) 42px!important;gap:6px!important;align-items:center!important}.stem-isolator .stem-toggle{min-width:0!important;padding:7px 5px!important;font-size:8px!important;white-space:nowrap!important}.stem-isolator .stem-range{width:100%!important}.stem-isolator .stem-value{font-size:9px!important;text-align:right!important}.stem-isolator [data-stem-jecker-split],.stem-isolator .stem-jecker-split{display:none!important}@media(max-width:1000px){.stem-isolator .stem-row{grid-template-columns:72px minmax(0,1fr) 34px!important;gap:3px!important}.stem-master-primary{min-width:62px!important;padding:4px 6px!important}}';d.head.appendChild(style);
      const syncUi=()=>{const states=engine.status?.().states||{};keys.forEach(key=>{const button=iso.querySelector(`[data-stem-toggle="${key}"]`),range=iso.querySelector(`[data-stem-range="${key}"]`),output=iso.querySelector(`[data-stem-value="${key}"]`),state=states[key]||{};if(button){button.classList.toggle('active',!!state.on);button.setAttribute('aria-pressed',String(!!state.on))}const pct=Math.round(clamp(state.level)*100);if(range&&Math.abs((parseFloat(range.value)||0)-pct)>.5)range.value=String(pct);if(output)output.textContent=pct+'%'});if(master){const statesNow=engine.status?.().states||{},on=keys.some(k=>!!statesNow[k]?.on);master.classList.toggle('active',on);master.setAttribute('aria-pressed',String(on))}};
      const propagate=()=>{const jecker=w.__enochStemJecker,api=w.__enochDoubleDeckerSpecial;if(!jecker?.linked||!api)return false;const states=engine.status?.().states||{};keys.forEach(key=>{api.setStemOn?.('A',key,!!states[key]?.on);api.setStemOn?.('B',key,!!states[key]?.on);api.setStemLevel?.('A',key,clamp(states[key]?.level));api.setStemLevel?.('B',key,clamp(states[key]?.level))});api.enforceActivePlayback?.(false);return true};
      keys.forEach(key=>{const button=iso.querySelector(`[data-stem-toggle="${key}"]`),range=iso.querySelector(`[data-stem-range="${key}"]`);if(button&&!button.dataset.fourStemBound){button.dataset.fourStemBound='v4';button.addEventListener('click',event=>{event.preventDefault();event.stopImmediatePropagation();const state=engine.status?.().states?.[key];engine.setRow?.(key,!state?.on);syncUi();propagate()},true)}if(range&&!range.dataset.fourStemBound){range.dataset.fourStemBound='v4';range.addEventListener('input',event=>{event.stopImmediatePropagation();engine.setLevel?.(key,clamp((parseFloat(range.value)||0)/100));syncUi();propagate()},true)}});
      const patchJecker=()=>{const jecker=w.__enochStemJecker;if(!jecker||jecker.__fourNativePatched)return false;jecker.sync=()=>{propagate();return true};jecker.liveMask=()=>{const states=engine.status?.().states||{};return Object.fromEntries(keys.map(key=>[key,!!states[key]?.on]))};jecker.__fourNativePatched=true;return true};
      const timer=w.setInterval(()=>{iso.querySelectorAll('[data-stem-jecker-split],.stem-jecker-split').forEach(n=>n.remove());syncUi();patchJecker();propagate()},250);
      d.documentElement.dataset.stemFourChannel='v4';w.__enochStemFourChannel={version:'v4',sync:syncUi,propagate,patchJecker,master};syncUi();patchJecker();propagate();w.addEventListener('pagehide',()=>w.clearInterval(timer),{once:true});return true;
    }catch(_){return false}
  }
  window.installEnochianStemFourChannelV1=frame=>{let n=0;const t=setInterval(()=>{if(install(frame)||++n>240)clearInterval(t)},50);return install(frame)};
})();
