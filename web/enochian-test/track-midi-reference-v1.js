(()=>{
'use strict';
const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
const deckDoc=frame=>{try{return frame?.contentDocument?.getElementById('deck')?.contentDocument||null}catch(_){return null}};
const readVLQ=(u,s)=>{let v=0,b=0,i=s;do{b=u[i++];v=(v<<7)|(b&127)}while((b&128)&&i<u.length);return [v,i]};
const u32=(u,i)=>(u[i]<<24)|(u[i+1]<<16)|(u[i+2]<<8)|u[i+3];
function parseMidi(u){if(String.fromCharCode(...u.slice(0,4))!=='MThd')throw Error('bad midi');const ppq=(u[12]<<8)|u[13],tracks=(u[10]<<8)|u[11];let p=14,events=[],tempos=[{tick:0,tempo:500000}];for(let tr=0;tr<tracks;tr++){if(String.fromCharCode(...u.slice(p,p+4))!=='MTrk')break;const len=u32(u,p+4)>>>0,end=p+8+len;let i=p+8,tick=0,status=0;while(i<end){let q=readVLQ(u,i);tick+=q[0];i=q[1];let st=u[i++];if(st<128){i--;st=status}else status=st;if(st===255){const type=u[i++];q=readVLQ(u,i);const n=q[0];i=q[1];if(type===81&&n===3)tempos.push({tick,tempo:(u[i]<<16)|(u[i+1]<<8)|u[i+2]});i+=n;continue}if(st===240||st===247){q=readVLQ(u,i);i=q[1]+q[0];continue}const cmd=st&240,ch=st&15;if(cmd===192||cmd===208){i++;continue}const a=u[i++],b=u[i++];if(cmd===144&&b>0)events.push({tick,on:1,note:a,vel:b,ch});else if(cmd===128||(cmd===144&&b===0))events.push({tick,on:0,note:a,vel:0,ch})}p=end}tempos.sort((a,b)=>a.tick-b.tick);const secs=t=>{let sec=0,last=0,tempo=500000;for(const x of tempos){if(x.tick>t)break;sec+=(x.tick-last)*tempo/(ppq*1e6);last=x.tick;tempo=x.tempo}return sec+(t-last)*tempo/(ppq*1e6)};events.forEach(e=>e.time=secs(e.tick));events.sort((a,b)=>a.time-b.time);return {ppq,events,duration:events.length?events[events.length-1].time:0,tempos}};
function install(frame){const d=deckDoc(frame),w=d?.defaultView;if(!d||!w)return false;if(d.documentElement.dataset.trackMidiReference==='v1')return true;const audio=d.getElementById('audio'),name=d.getElementById('name'),track=d.getElementById('track'),bus=w.__enochAnalyserBus;if(!audio||!bus?.emit)return false;const state={version:'v1',title:'AI Comptroller',asset:'/enochian-test/midi/ai_comptroller.mid.b64',midi:null,held:new Map(),index:0,lastTime:-1,scale:1,active:false};
const emit=(e,on)=>{const vel=on?e.vel:0,freq=440*Math.pow(2,(e.note-69)/12),energy=vel/127;bus.emit('midi-note',{source:'track-midi',track:state.title,note:e.note,velocity:vel,frequency:freq,on:!!on,time:audio.currentTime});if(on){state.held.set(e.note,vel);bus.emit('mod-sculpt-input',{source:'track-midi',energy:Math.max(.18,energy),pullY:clamp((e.note-60)/36,-1,1),pullZ:clamp((energy-.35)*1.7,-1,1),twist:clamp((e.note%12-5.5)/11,-.5,.5),note:e.note,velocity:vel})}else state.held.delete(e.note);const spectrum=[...state.held].map(([note,velocity])=>({note,velocity,frequency:440*Math.pow(2,(note-69)/12)}));bus.emit('midi-spectrum',{source:'track-midi',track:state.title,notes:spectrum,energy:spectrum.length?Math.max(...spectrum.map(x=>x.velocity))/127:0})};
const isTarget=()=>((name?.textContent||track?.selectedOptions?.[0]?.textContent||'').trim().toLowerCase().includes('ai comptroller'));
const reset=t=>{for(const [n] of state.held)emit({note:n,vel:0},false);state.held.clear();state.index=0;const mt=(t||0)/Math.max(.0001,state.scale);while(state.midi&&state.index<state.midi.events.length&&state.midi.events[state.index].time<mt)state.index++;state.lastTime=t||0};
const tick=()=>{state.active=isTarget()&&!!state.midi;if(state.active){const dur=Number(audio.duration);state.scale=Number.isFinite(dur)&&dur>1&&state.midi.duration>1?dur/state.midi.duration:1;const t=audio.currentTime||0;if(state.lastTime<0||t<state.lastTime-.15||t-state.lastTime>1.5)reset(t);if(!audio.paused&&!audio.ended){const mt=t/state.scale;while(state.index<state.midi.events.length&&state.midi.events[state.index].time<=mt+.025){const e=state.midi.events[state.index++];emit(e,!!e.on)}}state.lastTime=t}w.requestAnimationFrame(tick)};
fetch(state.asset,{cache:'no-store'}).then(r=>r.text()).then(s=>{const raw=atob(s.trim()),u=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)u[i]=raw.charCodeAt(i);state.midi=parseMidi(u);state.index=0;state.lastTime=-1;d.documentElement.dataset.trackMidiLoaded='ai-comptroller';bus.emit('track-midi-ready',{source:'track-midi',track:state.title,events:state.midi.events.length,duration:state.midi.duration,ppq:state.midi.ppq})}).catch(()=>{d.documentElement.dataset.trackMidiLoaded='error'});
audio.addEventListener('seeking',()=>reset(audio.currentTime));audio.addEventListener('emptied',()=>reset(0));track?.addEventListener('change',()=>reset(audio.currentTime));w.__enochTrackMidiReference=state;d.documentElement.dataset.trackMidiReference='v1';w.requestAnimationFrame(tick);return true}
let timer=0;window.installEnochianTrackMidiReferenceV1=frame=>{if(install(frame)){if(timer)clearInterval(timer);timer=0;return true}if(!timer){let n=0;timer=setInterval(()=>{if(install(frame)||++n>180){clearInterval(timer);timer=0}},100)}return false};
})();