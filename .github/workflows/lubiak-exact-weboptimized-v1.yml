name: LUBIAK exact WEB_OPTIMIZED v1

on:
  push:
    branches: [main]
    paths:
      - '.lubiak-exact-weboptimized-v1-trigger'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply modular LUBIAK runtime path
        run: node scripts/patch-lubiak-exact-weboptimized-v1.mjs
      - name: Validate modular model contract
        shell: bash
        run: |
          set -euo pipefail
          node --check web/lubiak/lubiak.js
          grep -q 'textured-glb-comparison/LUBIAK_REASSEMBLED_MODULAR_WEB.glb' web/lubiak/lubiak.js
          grep -q 'LUBIAK_REASSEMBLED_MODULAR_WEB.glb' web/lubiak/index.html
          ! grep -q '/assets/assets/models/LUBIAK.glb' web/lubiak/lubiak.js
          test -s assets/models/textured-glb-comparison/LUBIAK_REASSEMBLED_MODULAR_WEB.glb
      - name: Request deployment
        run: |
          cat > .github/deploy-now.txt <<'EOF'
          deploy-request: 2026-09-03T11:00:00+01:00
          source: chat
          workflow: WEB — Deploy Live Site
          scope: LUBIAK modular comparison master test
          reason: load only LUBIAK_REASSEMBLED_MODULAR_WEB.glb directly in LUBIAK and deploy
          EOF
      - name: Commit repair
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add web/lubiak/lubiak.js web/lubiak/index.html .github/workflows/deploy-pages.yml .github/deploy-now.txt
          git diff --cached --quiet && exit 0
          git commit -m 'Display LUBIAK modular comparison master'
          git pull --rebase origin main
          git push origin HEAD:main
